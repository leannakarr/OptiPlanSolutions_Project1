the data base acess's sqLight data base useing sql.js-httpvfs method and code.
the functioning tables can only be refrenced so JSON files are used for
updatable data. data base can be transfered to more functionable acess method 
when needed. 
